<div class="container">
	<div class="raw">
		<div class="col-md-8 col-md-offset-2">
			<% @users.each do |user| %>
			<%= attachment_image_tag user, :user_image, :fill, 150, 150, format: 'jpeg' %><br>
			<%= user.name %>
			<% end %> 
		</div>
	</div>
</div>
