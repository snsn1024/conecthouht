<div class="body">
	<div class="container">
		<div class="raw">
			<div class="col-md-3 col-md-offset-1 well">
				follow
			</div>
			<div class="col-md-4 well">
				tree
			</div>
			<div class="col-md-3 well">
				<%= @user.name %> <%= link_to "edit",edit_user_path(current_user.id) %>
				<%= attachment_image_tag @user, :user_image, :fill, 245, 245, format: 'jpeg' %>
				<%= @user.profile %>
			</div>
		</div>
	</div>
</div>
